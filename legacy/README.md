# Legacy Implementations

This directory contains previous `cc-bridge` implementations and artifacts that are no longer the active runtime:

- Go server
- Python bridge helpers
- old compiled Go binary
- old bundled frontend assets

The active backend is the Rust implementation in `../src/`.

Do not use files in this directory as the default entrypoint for new work.
