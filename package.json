{
  "name": "sparky-proma",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "web:dev": "cd apps/web && ./node_modules/.bin/vite",
    "web:build": "cd apps/web && ./node_modules/.bin/vite build",
    "server:dev": "cd apps/server-go && go run ./cmd/server",
    "server:start": "cd apps/server-go && go run ./cmd/server"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "typescript": "^5.0.0"
  }
}
