# Sparky 操作指引

## 📋 目录

- [项目简介](#项目简介)
- [前置准备](#前置准备)
- [飞书开放平台配置](#飞书开放平台配置)
- [桌面应用使用指南](#桌面应用使用指南)
- [常见问题](#常见问题)

---

## 项目简介

**Sparky** 是一个基于 Rust + Tauri 开发的现代化 Claude Code 桌面环境监视器。它内置了完整的终端环境 (PTY)，让你可以在图形化界面中管理多个开发项目，同时通过飞书开放平台的 WebSocket 长连接实现远程监控与控制响应。

### ✨ 核心特性

- 🖥️ **全功能终端界面**：应用内嵌真正的终端引擎，支持日志回看与状态监控。
- 🔌 **零配置长连接**：彻底告别公网 IP 面板和内网穿透软件，使用内置 WebSocket 长连接通道。
- 📱 **飞书智能卡片推送**：精准识别 Claude Code 的权限请求事件并推送到飞书。
- 🔐 **快捷交互验证**：在飞书群或私聊内回复快捷选项编码（如 `91-1`），后端自动代理转化为方向键并送入控制台。
- 💾 **无感持久化**：SQLite 为底座，记录您的活跃项目状态并持久保留全局设置。

---

## 前置准备

### 1. 系统要求

- **操作系统**：macOS 10.15+、Windows 10+ 或 Linux Desktop
- **软件环境**：
  - Node.js 16+ (用于前端构建)
  - Rust 1.70+ (用于 Tauri 后端层与核心 Hook CLI)

### 2. 检查开发环境 (如果从源码构建)

```bash
# 检查 Node.js
node --version

# 检查 Rust 和 Cargo
rustc --version
cargo --version
```

---

## 飞书开放平台配置

### 步骤一：创建应用

1. **访问飞书开发者后台**
   - 进入 https://open.feishu.cn/app
   - 登录您的飞书工作台

2. **创建企业自建应用**
   - 点击“创建企业自建应用”
   - 填写应用名称：`Sparky Bot` (或自定义)
   - 填写应用描述：`我的 Claude Code 远程助手`
   - 上传图标并点击“创建”

### 步骤二：配置机器人能力

1. **开启机器人**
   - 在左侧菜单找到“添加应用能力”，添加“机器人”功能。

2. **申请权限**
   在“权限管理”页面，申请并开通以下权限（需要进行版本发布审核）：
   - `获取与发送单聊、群组消息` (im:message)
   - `接收群聊中@机器人的消息` (im:message.group_at_msg)
   - `接收单聊消息` (im:message.p2p_msg)

### 步骤三：开通长连接事件订阅

1. **进入事件配置**
   - 在“事件与回调”页面，点击“添加事件”
   - 勾选 `接收消息` (im.message.receive_v1)

2. **配置回调方式为长连接**
   - 选择 **“使用长连接接收事件”**
   - *（注：千万不要选择 HTTP 回调，Sparky 不需要内网穿透端口映射！）*

### 步骤四：获取应用凭证

进入“凭证与基础信息”页面，您需要记录以下两项用于 Sparky 桌面端配置：
- **App ID**：形如 `cli_xxxxxxxxxxxxxxxx`
- **App Secret**：您的密钥串

最后，记得点击 **“创建版本”** 并提交审核。审核通过后才能真正在群里启用机器人。

---

## 桌面应用使用指南

Sparky 抛弃了过去杂乱的环境变量和繁琐的服务启动，现在开箱即用。

### 1. 编译并启动 Sparky

```bash
# 如果使用开发模式：
./start-dev.sh

# 等待 Cargo 和 Vite 构建，随后会自动弹出漂亮的桌面窗口。
```

如果您希望编译成独立的生产 App 分发：
```bash
./build.sh
# macOS 可以在 src-tauri/target/release/bundle/macos 下找到编译好的 .app 文件
```

### 2. 初始化应用配置

1. 打开 Sparky 桌面应用。
2. 在左侧菜单栏点击进入 **“设置”** 页面。
3. 填入上文中从飞书开发者后台获取的：
   - **App ID**
   - **App Secret**
4. 选填推送目标：
   - 填写您的 `Open ID` (私聊) 或 `Chat ID` (推送到群发)。
5. 勾选需要推送的提醒类型（如 `权限确认`、`运行结束` 等）。
6. 点击上方操作区的 **“保存配置”**。
   - 成功后，界面顶部的连接状态会立刻从“未连接”变成绿色的 **“已连接”** 状态。您的 WebSocket 通道已建立。

### 3. 项目与终端管理

1. 打开左侧导航栏的 **“项目”** 页面。
2. 点击 **“添加项目”**，选择您打算运行 Claude 的具体代码根目录。
3. 选择项目后如果出现黄色的“未安装”，请点击 **“配置”**——Sparky 将自动会在您的项目文件夹写入 `.claude/settings.local.json` 把 Hook 入口指向本地的免打扰探针！
4. 准备就绪后，点击 **“Go >”**，即可进入项目的独立控制台。
5. 在终端框内输入正常的指令，比如：
   ```bash
   claude
   ```

### 4. 远程拦截工作流测试

1. 终端中的 Claude Code 工作一段时间后，当它询问 `Do you want to proceed? [y/N]` 以及工具多选列表时，它将被打断阻塞。
2. 此时，您的飞书端会立刻收到卡片推送通知，告知权限受限并附上当前的匹配码（例如“**🔑 配对码: 91**”）。
3. 如果是在公司路上，您只需在飞书群/私聊回复该机器人的消息即可：
   - 输入 `91-1`（同意第一个选项，通常就是 Y）
   - 输入 `91-2`（通常是同意并始终信赖此工具）
   - 输入 `91-3`（拒绝执行）
4. 返回电脑旁，您会发现控制台屏幕已经自动输入对应的回车，且 AI 继续向后运行了，没有任何停滞！
5. 不再需要该项目时，点击红色的电源 **关闭终端** 按钮，系统会安全释放后台进程引擎。

---

## 常见问题

### Q1: 飞书配置界面的连接状态一直是“未连接”怎么办？

**解决方案**：
1. 请核对您的 App ID 与 App Secret 是否精确无误地粘贴到了 Sparky 配置页，并且**已保存**。
2. 请确认飞书后台的“事件订阅”中确实选为了长连接，且包含相应的权限申请，应用发布已通过审查。
3. 如果仍有问题，可通过 `运行中:` 的悬浮终端尝试重新载入进程。

### Q2: 终端打印日志为什么比以前的 CLI 版少？

**设计初衷**：
Sparky 应用将内部通讯级的日志流设置为了 `ERROR`，目的是为了防止底层 Hook 组件把调试级日志强行推给上层交互终端导致被误当做提示词上下文给大模型处理。
所有执行详情与 WebSockets 数据包收发日志都会静默写入您计算机的 `~/sparky-server/sparky-server.*.log` 中。如果有需要详细核对报错的，请查阅此文件夹的文本日志。

### Q3: 为什么回复 26-y 没有任何反应？

由于交互式伪终端 (PTY) 程序不再接受无回车的字母输入，所有带编号的交互提示框请使用 `配对码-选项数字`（如 `26-1`, `26-2`）形式进行回复，这样后端会智能将选择序号转换为方向键上下移动供终端选中指定选单！

---
*Powered by Rust, Tauri, and React. Built for efficiency.*
